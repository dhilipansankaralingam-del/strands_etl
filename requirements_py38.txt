# Strands ETL Framework - Requirements (Python 3.8+ Compatible)
# Note: Some features may require Python 3.10+

# Core dependencies
boto3>=1.26.0
botocore>=1.29.0

# Data processing
pandas>=1.5.0
numpy>=1.23.0
pyarrow>=10.0.0

# AWS Glue (for local development)
# Note: awsglue module only available in Glue environment

# Configuration
pyyaml>=6.0

# CLI tools
click>=8.0.0

# Utilities
python-dotenv>=0.19.0
requests>=2.28.0

# Strands Agents SDK - Try installing with lower Python version
# If this fails, the framework will work without it (agents won't be available)
strands-agents>=1.23.0; python_version >= "3.10"

# Alternative: For Python 3.8/3.9, use basic agent functionality without Strands SDK
# The config-driven framework will still work with AWS services

# Development dependencies (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.950

# Notebook support (optional)
jupyter>=1.0.0
ipython>=8.0.0
